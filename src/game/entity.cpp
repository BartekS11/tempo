#include "../../include/game/entity.h"

// void RenderEntities(SDL_Renderer* pRenderer, size_t entitiesCount)
// {
//     for(size_t i = 0; i < entitiesCount; i++) {
//         BS_ASSERT(entities[i].render, "Entity %zu has no render function",
//         i); entities[i].render(pRenderer);
//     }
// }

// void UpdateEntities(size_t entitiesCount, bs_float dt)
// {
//     for(size_t i = 0; i < entitiesCount; i++) {
//         BS_ASSERT(entities[i].update, "Entity %zu has no update function",
//         i); entities[i].update(dt);
//     }
// }
